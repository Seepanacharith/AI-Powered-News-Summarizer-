# Trigger rebuild with dynamic port config
services:
  - type: web
    name: ai-news-backend
    env: node
    plan: free
    rootDir: Server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: SECRET_KEY
        value: SoftwareEngineering
